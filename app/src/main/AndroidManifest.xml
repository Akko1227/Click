<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xutin_000.finalexam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"> <!-- 在第一行中配置LitePalApplication：android:name="org.litepal.LitePalApplication" -->
        <activity
            android:name=".MainActivity"
            android:label="FinalExam" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".RankActivity" />
        <activity android:name=".ShopActivity"></activity>
    </application>

</manifest>